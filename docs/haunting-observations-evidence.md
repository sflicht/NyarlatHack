# Phase-one observation evidence and publication status

## Current local acceptance — 8f317763a

**Independent PASS for the stated local execution/behavioral gates at
`8f317763a3560bd774d0b61e134e7028a744412a`**, source tree
`af00c9999d61ee5c641a78535ade23e1fd1ad578`, under the approved
[`provenance-validated-dump-v1` contract](turnloop-dump-comparison-contract.md).
Approval is recorded as message `1549898563021054044`; no new permission or
independent Discord retrieval is implied. Local acceptance is not publication,
hosted continuous integration (CI), pull-request/merge success or consequential
gameplay. This ledger reconciles retained execution; it performs no new run.

The independent review is `/tmp/nyarl-mode-fresh-acceptance-review.md`.
Its exact-revision suite has **679 unique test IDs: 672 passed, seven skipped,
no failures/errors, 303.455 seconds**. Three skips require ownership privileges;
four are default artifact-dependent fountain-oracle invocations. The configured
matrix-oracle child separately executed seven methods with zero skips; these
are not seven missing gates or seven additional top-level tests. The selected
turn-loop driver, all three native strict-oracle sensitivity methods, configured
historical mutations, native curio save/restore and expired-director continuation
actually executed successfully, rather than being inferred from registration.

**The native unittest command returned zero; the outer reporting wrapper exited
1.** `/tmp/nyarl-mode-fresh-build.json` remains `status:failed`: the wrapper's
single-line parser rejected the curio test's `CURIO8E2_ARTIFACTS=...` output
followed by `ok` on the next line. The supplemental
`/tmp/nyarl-mode-fresh-postvalidation.json` and independent block parsing establish
suite success; neither replaces that failed receipt nor represents a rerun.
Earlier full-suite failures also remain failures: the `46b4c364f` run at
`/tmp/nyarl-provenance-fresh-2rohguef/output/full_suite.log` recorded 651 tests,
13 failures and seven skips; the `cd303498b` run at
`/tmp/nyarl-reaping-fresh-706xxlsi/output/full_suite.log` recorded 673 tests,
one failure and seven skips.

### Selected-command acceptance and its boundaries

All seven configurations ran: `reviewed-chaos0`, `inactive`, `legacy-empty`,
`legacy-repeat`, `v2-empty`, `v2-repeat`, `upstream-stock`. Inputs, raw terminal
and xlog bytes are exact across all seven. Every dump byte is exact except the
cross-build line-2 header content validated against independent build evidence;
all five current CHAOS=1 full dumps are exact. Current headers derive from
retained per-mode generated date captures and original receipts, not the compared
dumps. Historical stock retains its independently reviewed receipt/dump/license
pins. This is cooperative receipt provenance, not hostile same-user attestation.

The native main/turn-loop route witnesses ordinary whistle, magic whistle with
pet present, actual potion consumption after fountain decline, and water-snakes
fountain aftermath. It does not prove whole-loop pet relocation. The v2 history
has two whistle roots and one fountain root, no potion root; legacy/v2 repeats
are exact. The selected local gate is accepted by independent review under the
approved alternate contract, **not literal strict full-file comparison**.
Both original and new strict `result.json` remain failed. The supplemental
receipt retains `task8_closed:false`; manual acceptance is separate and does not
rewrite the driver's deliberately conservative machine field.

The fresh linked fountain matrix contains 74 normal actions (30 ordinary fates
and seven special paired cases) plus three negative controls. Selected-action
transport has 16 fault rows, four healthy/off rows and six negatives, not whole
turn-loop coverage. Seven outer supervisor receipts (platform, whistle, fountain,
matrix, matrix oracle, transport, turnloop) record returncode 0 and verified
family cleanup. This is not universal process containment or EOF/error safety.

The real native curio fresh/explicit-restore pair records charges 2→1, state
1→2, Sanity 98→99, owner 114 and spent 2 unchanged, with native save/record bytes,
input/log and source coupling. Its source-build helper pin (`SOURCE_DRIVER_HASH`)
is `d22eae5cb3f1610a8f950c4c4ebc95af99296db0a02e1c0b967c8f164bb95ccc`;
historical `DRIVER_HASH` remains
`9d341b28a4ab3f4453b09e4f49a2e8701a7c78345184f487e2f0b32d70db7270`.
Mode-specific selection includes native layout calibration, not just a source hash.
Across the inspected `49d4e166d`→`8f317763a` interval, the only changed path under
`src/include/chaos/GNUmakefile/dat` is `chaos/README.md`; no C/rule/RNG/new-mutation
change is inferred beyond that bounded inspection.

### Current traceability

Local paths, not hosted artifacts: **O** is
`/tmp/nyarl-mode-fresh-qo070gz1/output`; **I** is
`O/fixtures/full_suite-0kupvop_`; **C** is
`O/fixtures/curio8e2-native-q31npar8`. Current products remain present; older
retirement notes below describe only their historical runs. Mode-0 intermediate
objects were superseded during sequential builds, but its separate date capture
and executable/data/license tuple were retained and verified.

SHA-256 fingerprints checked against retained bytes:

| Record | SHA-256 |
|---|---|
| `O/full_suite.log` | `261bc4c6cc2f343c5cbf4c002910b94929701780ae2f30719c54362120e8f336` |
| `/tmp/nyarl-mode-fresh-build.json` (failed wrapper) | `324e2af5cc78932b243bc7dcdc041f042f116b7283f20e8d53d0ba9b5409a362` |
| `I/turnloop/provenance-result.json` | `c8862a878ad6144a3a05a00944821461b22da71204ea341db7ad4c2b3b6ee8f5` |
| `I/turnloop/result.json` and original `/tmp/nyarl-episode-turnloop-work/final/result.json` | `c9a75d556e7598f5597cbfa1d9d56127b293465bc59d64316c408badda25326c` |
| `O/system-gcc13/0-manifest.json` | `000ab8555a7dacc3ee2f73b4faa1bcb35d7a14347ff78459926801d2cf027021` |
| `O/system-gcc13/1-manifest.json` | `52ea7184f9b987b634e1b54981924e69c97b7283c03d20af8a0d2a8621754db1` |
| `/tmp/nyarl-mode-fresh-acceptance-review.md` | `079e24aace63a41b557a9cd0d86af33914454734d5e24fa9715e32ea4ef3a498` |

Remaining work is finite: final documentation/receipt reconciliation and review,
then PR publication and verification of required hosted lint/build/native/history
checks and final sign-off. No issue (#21, #26 or #27) is closed here. This adds no
history-fed Luna decision, new mechanic, budget, model integration or unified
launcher, and claims neither arbitrary-world safety nor human comprehension.
Bulk receipts and private metadata are not committed or claimed uploaded.

## Historical accepted local scope — 49d4e166d

The earlier accepted configured fresh local scope, not whole-phase completion,
refers to `49d4e166d001d27072ba9b50f5854a70a8e70cb7`, source tree
`4cf8d9b5492ea20944a43b1821ba10e3e2f68caa`. It summarizes reviewed execution,
not a new game run performed while writing documentation. Hosted continuous
integration (CI) status is unknown; no pull-request or hosted success is claimed.

### Earlier suite accounting

| Evidence | Recorded result and boundary |
|---|---|
| Fresh configured discovery | 618 tests in 287.257 seconds; 607 passed, 11 top-level skips; exit 0. |
| Skip accounting | Three ownership/privilege skips; four default-discovery matrix artifact methods exercised separately; four unaccepted turn-loop methods. |
| Explicit supervised matrix oracle | Seven distinct methods actually ran, zero skips/errors/failures. Includes the four matrix artifact methods above; do **not** add these nested tests to 618. |
| Fountain paired native matrix | All 30 ordinary fates plus seven special cases: 74 healthy actions and three separate negatives. Native draw, raw continuation and state/budget negatives reject. |
| Selected-action transport | 16 fault rows, four healthy/off rows, six negatives. Real linked actions and continuation, **not** the whole native turn loop. Failed synchronization is distinguished from a committed event prefix. |
| Platform/whistle isolation | Accepted subprocess isolation of process-global umask and hard resource limits; source wiring alone is not this native evidence. |

The fresh review compared all 77 fountain rows and 26 transport rows with
retained native/state/event evidence. These bounded contracts do not establish
all-dungeon safety, all possible outcomes or unlimited information-flow safety.
Production `src`, `include`, `chaos`, `GNUmakefile` and `dat` are unchanged from
`170c68e502a91662df832ec6029603915f0a1666` to the revision above; acceptance
wiring changed. Production identity is not the same as fresh execution of every
old fixture at the new revision.

### Controlled fatal interruption is covered, separately

The retained controlled fatal run on the `170c68e50` production baseline reached
the real fountain action and native contaminated-water poison death: gameover,
HP -4, action entered and did not return, three action RNG draws. Its stream
ends with selected `started` then legacy final death, no terminal; projection
has incomplete=1 and no positive episode. This satisfies **Task 7's fatal-
interruption requirement**. Setup occurs before the real call, with only main
and dodrink wrapped; it is not the stricter Task 8 count/forward-only oracle.
No Task 8 wrapper exception is needed to count this Task 7 evidence.
It is **not** fresh native-fatal execution at `49d4e166d` or `8f317763a`, a newly registered
fatal CI gate, or strict whole-turn-loop acceptance.

Existing linked coverage includes decline/cancel, Levitation bypass, explicit
no-mouth, and whistle wake/companion-state/relocation/trap aftermath. The native
turn-loop route also performs actual potion ingestion after declining the
fountain, with parsed event equality and no potion root. It does not retain a
separate raw before/after snapshot pair. Its magic-whistle witness is **pet
present**, not demonstrated relocation; linked aftermath supplies that separate
requirement. There is no requirement to repeat every linked fountain world or
whistle aftermath through all whole-game configurations. Equal context pairs
must not be described as proof of changed vitals.

## Historical strict failure — preserved

**The original strict turn-loop comparison remains FAILED, exit 1.** Its frozen
native revision is `170c68e502a91662df832ec6029603915f0a1666`, not the fresh
suite revision. Four selected command paths—ordinary whistle, magic whistle,
fountain decline followed by actual potion consumption, confirmed fountain—ran
through original native main/moveloop in seven configurations:
`reviewed-chaos0`, `inactive`, `legacy-empty`, `legacy-repeat`, `v2-empty`,
`v2-repeat`, `upstream-stock`.

Input, terminal and xlog bytes agree; legacy and v2 repeats agree. Cross-build
final dumps differ on line 2, the native build-identification line. The diagnosis
is metadata-only; **the strict whole-file comparator still fails**. At the time
of this earlier run, no approved alternate comparison existed and exact-current-
revision turn-loop acceptance was pending: the `49d4e166d` suite skipped the
selected-command driver and its three native-artifact oracle methods. The approved
alternate and exact `8f317763a` acceptance above supersede that pending status,
not this failure. The confirmed fountain in
the frozen route is fate-22 water snakes followed by survival and quit, not the
separately covered refreshment or fatal case.

The original failure remains unchanged. Final documentation/receipt review and
publication remain open; exact-source selected-terminal acceptance is now recorded
above. Neither this ledger nor the fresh local pass declares the whole phase
published. No new mechanic, Lua/Luna change,
budget change, ordinary-play recurrence, whole #21 completion or consequential
vision (#26) is established by this observation foundation.

## Historical traceability and reproduction

Use the portable [fresh same-checkout recipe](quality-control.md#local-equivalents)
with an independently reviewed full revision and your own disposable paths.
No public user needs the review machine's absolute paths. The recipe covers the
core local suite; it is not the complete configured turn-loop invocation and
does not turn strict full-file failure into success. The [protocol](chaos-protocol.md) and
[sidecar guide](../chaos/README.md#offline-selected-action-observations) describe
supported use. The unchanged [specification](superpowers/specs/haunting-observations-phase1.md)
and [plan](superpowers/plans/haunting-observations-phase1.md) remain the acceptance
contracts, not declarations of completed work.

Retained local review records (basenames for audit traceability, not public
links): `nyarl-expanded-fresh-acceptance-review.md` (lines 11–30 source/build,
34–69 execution, 74–78 limits), `nyarl-phase1-remaining-gates-after-fresh.md`
(lines 11–38 fatal and strict-gate disposition),
`nyarl-turnloop-dump-contract-review.md` (lines 13–38 byte mismatch), and
`nyarl-expanded-suite-cleanup-report.md` (lines 3–29 retirement/preservation).
The later remaining-gates audit supersedes the older dump review's open-fatal
list and obsolete optimized-Python finding.

Selected SHA-256 fingerprints copied from the accepted review and checked against
retained records; artifact paths are relative to the fresh output/invocation,
not promises of hosted availability:

| Record | SHA-256 |
|---|---|
| `full_suite.log` | `b6d1557015f78dbf002fa19e93e87b74ffb6172f6ea7008db675ace7d637356b` |
| `system-gcc13/0-manifest.json` | `a2f063c2a5b5e688d0150a64e4d1e4ded2ab3cb97a191d1ffec6396f8452c17a` |
| `system-gcc13/1-manifest.json` | `3538e8b8abca526799259a14a37eb79baa6256505aeec94be565efe5049b5f49` |
| `fountain-matrix/oracle-execution.json` | `edb5f750c35179af020772ab030ae179f9bbf1ab10c8e5e5f87f12730dd70197` |
| `fountain-matrix/result.json` | `33132cb6a2cd583de5982c0eed7ea50d4922f5d8275821f8add9b57b495ba2ba` |
| `action-transport/result.json` | `113ac555eb700e9d773fa8ff865a4e78801399caf73a02a804b33f498e06bf8f` |

Earlier baseline binaries/data were intentionally retired; later cleanup removed
all fresh-suite invocation ELF copies and duplicated `nhdat`, preserving the
original current core tuples, headers, production objects and text/state receipts.
Removed executables are historical hash provenance, not currently available
artifacts. Retirement does not retroactively invalidate the verified local run;
no restoration or rebuild was performed for this documentation. Mode-0
intermediate objects were superseded during the original sequential build and
are not claimed to survive. Bulk native logs, private host metadata and provider
records are deliberately not copied into this repository.
